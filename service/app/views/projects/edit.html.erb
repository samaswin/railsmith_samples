<main style="max-width: 700px; margin: 48px auto; padding: 0 16px;">
  <header style="display:flex; align-items: baseline; justify-content: space-between; gap: 16px;">
    <h1 style="margin: 0;">Edit project</h1>
    <%= link_to "Back", project_path(@project), style: "text-decoration:none; color:#111827;" %>
  </header>

  <section style="margin-top: 18px;">
    <%= render "form", project: @project %>
  </section>
</main>

